## builder

This directory contains support files for building self-contained
cross-compilation toolchains in docker images.  They're set up to
build either on a normal developer environment or in our CI environment.

